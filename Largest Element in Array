Largest Element in Array:

def largestElement(arr: [], n: int) -> int:
    largest=0
    arr.sort()
    for i in range(len(arr)):
        largest=arr[-1]
    return largest



def largestElement(arr: [], n: int) -> int:
    largest=arr[0]
    for i in range(1,n):
        if arr[i]>largest:
            largest=arr[i]
    return largest


